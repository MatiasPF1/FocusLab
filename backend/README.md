# Backend

This folder is reserved for a dedicated backend server (e.g. Express, FastAPI, or similar) if the project ever requires one separate from Next.js.

Currently, all server-side logic (API routes, database access, authentication) lives inside the Next.js app at `../frontend/`.
